include image

left = rectangle(64, 120, "solid", "blue")
middle = rectangle(64, 120, "solid", "white")
right = rectangle(64, 120, "solid", "red")

frenchFlag = overlay-xy(overlay-xy(left, 64, 0, middle), 128, 0, right)

frenchFlag